import React from "react";
import TopNav from "../components/TopNav";

const Navigation = ({ handleClose }) => {
  return (
    <>
      <TopNav handleClose={handleClose} />
    </>
  );
};

export default Navigation;
